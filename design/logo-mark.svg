<svg xmlns="http://www.w3.org/2000/svg" width="120" height="120" viewBox="0 0 120 120" fill="none">
  <!-- ClipRelay "Beam Simplified" — canonical geometry reference (no styling) -->

  <!-- Clipboard board -->
  <rect id="board" x="14" y="26" width="58" height="78" rx="10"/>

  <!-- Clamp base -->
  <rect id="clamp-base" x="26" y="18" width="34" height="12" rx="5"/>

  <!-- Clamp top -->
  <rect id="clamp-top" x="32" y="14" width="22" height="8" rx="4"/>

  <!-- Rivet -->
  <rect id="rivet" x="39" y="22" width="8" height="3" rx="1.5"/>

  <!-- Arc 1 (inner) -->
  <path id="arc-1" d="M78 44 A 24 24 0 0 1 78 82"/>

  <!-- Arc 2 (outer) -->
  <path id="arc-2" d="M90 35 A 36 36 0 0 1 90 91"/>
</svg>
